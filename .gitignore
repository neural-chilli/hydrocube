# Rust/Cargo build artifacts
/target/
Cargo.lock

# IDE files
/.idea/
*.swp
*.swo
*~

# OS-specific files
.DS_Store
Thumbs.db

# Local test data (created by run-test-env.sh)
/.local/

# Vendored front-end assets are committed so `git clone && cargo build` works
# without running scripts/vendor.sh. Re-run vendor.sh only when upgrading versions.

# Claude AI worktrees (project-specific)
/.claude/worktrees/